{
  "name": "tg-mini-app",
  "version": "1.0.0",
  "description": "Mini App for the Telegram Contest",
  "author": "Gleb Bayeshko",
  "license": "MIT",
  "scripts": {
    "start": "cd client && npm install && npm run build && cd ../server && npm install && npm run prod"
  }
}
